# 一键启动

Windows 启动：

```text
.\start.bat
```

Windows 停止：

```text
.\stop.bat
```

Linux / macOS 启动：

```bash
./scripts/bootstrap_venv.sh
./scripts/dev.sh
```

Linux / macOS 停止：

```bash
./scripts/stop-dev.sh
```

Vercel 发布：

```powershell
vercel.cmd deploy --prod --yes
```
