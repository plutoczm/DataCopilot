# 一键启动

Windows 启动：

```text
.\start.bat
```

自动使用 `D:\Anaconda\Miniconda3`，环境在项目根目录 `.conda`。

Windows 停止：

```text
.\stop.bat
```

Linux / macOS 启动：

```bash
./scripts/bootstrap_venv.sh
./scripts/dev.sh
```

Linux / macOS 停止：

```bash
./scripts/stop-dev.sh
```

Vercel 发布：

```powershell
vercel.cmd deploy --prod --yes
```
